# =====================================================================
# GFX GAMER LAB - Game List
# =====================================================================
# Add ONE game package name per line below.
# When any of these games is in the foreground, the module will:
#   1. Force the highest supported screen refresh rate
#   2. Set animations to 0.5x (faster UI, less GPU overhead)
#   3. Disable window blur / transparency effects
#   4. Close ALL other background apps (except system-critical ones)
#
# HOW TO FIND YOUR GAME'S PACKAGE NAME:
#   Run this in Termux (with adb connected) or any ADB terminal:
#     adb shell pm list packages | grep -i <part-of-game-name>
#   Example: adb shell pm list packages | grep -i pubg
#
# Common examples (uncomment the ones you use by removing the # ):
#com.pubg.imobile
#com.tencent.ig
#com.dts.freefireth
#com.activision.callofduty.shooter
#com.miHoYo.GenshinImpact
#com.garena.game.codm

# Add your game(s) below this line:
